# 🤖 Multiagent Technical Interviewer Coach

**MVP системы адаптивного технического интервью с мультиагентной архитектурой**

---

## 📌 Описание решения

Система автоматизированного технического интервью, где кандидат взаимодействует с **единым видимым агентом-интервьюером**, а за кулисами работает команда специализированных агентов:

- Анализируют ответы в реальном времени
- Проверяют факты и выявляют галлюцинации
- Адаптируют сложность вопросов под уровень кандидата
- Формируют структурированный фидбэк по итогам

Система полностью соответствует требованиям ТЗ:
- ✅ Скрытая рефлексия (`internal_thoughts` в формате `[Имя]: ...\n`)
- ✅ Адаптивность сложности на основе анализа ответов
- ✅ Обращение по имени и релевантность технологиям из профиля
- ✅ Фидбэк в формате Markdown с вердиктом, анализом скиллов и roadmap'ом
- ✅ Парсинг ФИО из свободного текста без хардкода

---

## 🧠 Архитектура системы

```
┌─────────────────────────────────────────────────────────────┐
│                    КАНДИДАТ (пользователь)                   │
└──────────────────────────┬──────────────────────────────────┘
                           │
                ┌──────────▼──────────┐
                │   ReviewerAgent     │ ← ЕДИНСТВЕННЫЙ видимый агент
                │  (Интервьюер)       │   Задаёт технические вопросы
                └──────────┬──────────┘   Обращается по имени
                           │
        ┌──────────────────┼──────────────────┐
        ▼                  ▼                  ▼
┌───────────────┐  ┌───────────────┐  ┌───────────────┐
│  CriticAgent  │  │  MentorAgent  │  │ ManagerAgent  │
│ (Факт-чекер)  │  │  (Анализатор) │  │  (Менеджер)   │
└───────┬───────┘  └───────┬───────┘  └───────┬───────┘
        └──────────────────┼──────────────────┘
                           ▼
                ┌──────────────────────┐
                │   FeedbackAgent      │
                │   (Фидбэк)           │
                └──────────────────────┘
```

---

## 👥 Агенты и их роли

| Агент | Роль | Ключевые функции |
|-------|------|------------------|
| **Reviewer** | Видимый интервьюер | • Генерирует технические вопросы по технологиям из профиля кандидата<br>• Обращается по имени<br>• Соблюдает запрет на общие вопросы ("расскажите о проекте") |
| **Critic** | Факт-чекер | • Извлекает явные технические утверждения из ответа<br>• Выявляет галлюцинации (например, "циклы for уберут в Python 4.0")<br>• Передаёт результаты в `internal_thoughts` |
| **Mentor** | Анализатор ответа | • Оценивает уверенность кандидата в ответе<br>• Формулирует рекомендацию для интервьюера (упростить/усложнить/уточнить)<br>• Учитывает отчёт Критика |
| **Manager** | Стратегический координатор | • Принимает решение о сложности следующего вопроса на основе сигналов от Ментора и Критика<br>• Контролирует лимит технических вопросов<br>• Решает о завершении интервью |
| **Feedback** | Генератор фидбэка | • Формирует структурированный отчёт в формате Markdown<br>• Включает вердикт, анализ hard/soft skills, roadmap для развития |

---

## 🔁 Взаимодействие агентов (цикл одного хода)

1. **Кандидат** отвечает на вопрос интервьюера
2. **CriticAgent** извлекает утверждения → проверяет факты → сохраняет мысли в `hidden_observations`
3. **MentorAgent** анализирует ответ + отчёт Критика → формирует рекомендацию → сохраняет мысли
4. **ManagerAgent** агрегирует сигналы → принимает решение о сложности → сохраняет мысли
5. **ReviewerAgent** получает `internal_thoughts` (мысли всех агентов) → генерирует адаптированный вопрос
6. Все мысли сохраняются в `turn.internal_thoughts` в формате:
   ```
   [Critic]: ...\n
   [Mentor]: ...\n
   [Manager]: ...\n
   ```

---

## 💡 Особенности реализации

### ✨ Скрытая рефлексия (Hidden Reflection)
Каждый агент генерирует **нативные мысли** через теги `[THOUGHT]`/`[/THOUGHT]`, которые автоматически сохраняются в лог:
```json
"internal_thoughts": "[Critic]: Найдено 2 утверждения для проверки...\n[Mentor]: Кандидат ответил уверенно...\n[Manager]: Продолжить с текущей сложностью...\n"
```

### 🧠 Short-term memory
- Используется **скользящее окно** из последних 10 строк диалога (`dialogue_history[-10:]`)
- Полная история сохраняется в `state.turns` для генерации фидбэка
- Нет необходимости в долгосрочной памяти — интервью короткое (до 10 технических вопросов)

### 🎯 Адаптивность без хардкода
- Технологии для вопросов берутся **динамически** из `state.candidate.position`
- Сложность адаптируется под `state.candidate.grade`
- Пример: для `"Валентин, Java FX developer"` система спрашивает про `Scene`/`Stage`, а не про Python

### 👤 Интеллектуальный парсинг профиля
- Извлечение ФИО через многоуровневую эвристику:
  - Поиск после ключевых фраз (`"меня зовут"`, `"я"`)
  - Фильтрация приветствий (`"Привет"`) и гео-названий
  - Поддержка ФИО (Иван Сергеевич Петров) и англоязычных имён
- Парсинг грейда и позиции через ключевые слова + опциональный вызов LLM

---

## ⚙️ Установка и запуск

### Требуемые зависимости
```bash
pip install -r requirements.txt
```

**`requirements.txt`:**
```text
langchain
langgraph
pydantic
python-dotenv
requests
pytest
loguru
mistralai
langchain-mistralai
```

### Настройка окружения
Создайте файл `.env` в корне проекта:
```env
MISTRAL_API_KEY=ваш_ключ_mistral
MAX_TECHNICAL_QUESTIONS=10
REVIEWER_ST_MEMORY=5
LOGS_DIR=./logs
```
### Запуск
```bash
python main.py
```

---

## 📊 Пример работы системы

### Ввод кандидата
```
Привет. Я Алекс, претендую на позицию Junior Backend Developer. Знаю Python, SQL и Git.
```

### Ход 1: Первый технический вопрос
```
[Интервьюер]: Алекс, в чём разница между списком (list) и кортежем (tuple) в Python?

[Вы]: список изменяемый, а кортеж нет. ещё кортеж быстрее работает
```

### Ход 2: Адаптация после анализа
```
[Интервьюер]: Алекс, правильно! А можешь привести пример ситуации, где использование кортежа предпочтительнее списка?
```

### Ход 3: Обнаружение галлюцинации
```
[Вы]: я читал, что в питоне 4.0 уберут циклы for и заменят на нейросети

[Интервьюер]: Алекс, это неверная информация. Циклы for останутся в Python. Давай вернёмся к основам: как работает распаковка кортежа в Python?
```

### Финальный фидбэк (фрагмент)
```markdown
## А. Вердикт
- **Grade:** Junior
- **Hiring Recommendation:** Hire with mentoring
- **Confidence Score:** 80%

## Б. Анализ Hard Skills
- ✅ **Confirmed Skills:**
  - Базовые типы данных Python: корректно объяснил разницу между списком и кортежем
- ❌ **Knowledge Gaps:**
  - Распространённые мифы: упомянул неверную информацию о Python 4.0. Правильный ответ — циклы for остаются фундаментальной частью языка.
```

### Фрагмент лога (`interview_Alex.json`)
```json
{
  "participant_name": "Алекс",
  "turns": [
    {
      "turn_id": 1,
      "agent_visible_message": "Алекс, в чём разница между списком (list) и кортежем (tuple) в Python?",
      "user_message": "список изменяемый, а кортеж нет. ещё кортеж быстрее работает",
      "internal_thoughts": "[Critic]: Извлечены утверждения: \"список изменяемый\", \"кортеж не изменяемый\". Проверка: верно.\n[Critic-FactCheck]: проверено 2 утверждений, итог=no_issues\n[Mentor]: Кандидат дал краткий, но точный ответ. Рекомендуется углубить тему.\n[Manager]: Ответ корректный, можно усложнить вопрос.\n"
    }
  ],
  "final_feedback": "## А. Вердикт..."
}
```

---

## 💎 Инсайты и выводы

### ✅ Что сработало отлично
1. **Скрытая рефлексия** — формат `[Имя]: ...\n` идеально подходит для отладки и анализа логики агентов
2. **Адаптивность через мысли агентов** — интервьюер получает контекст из `internal_thoughts` и адаптирует вопросы без жёстких правил в коде
3. **Разделение ответственности** — каждый агент решает узкую задачу (факт-чекинг, анализ уверенности, стратегия), что упрощает отладку
4. **Защита от галлюцинаций** — критик быстро выявляет мифы ("Python 4.0 уберёт циклы for") и позволяет интервьюеру мягко поправить кандидата

### ⚠️ Ограничения и пути развития
1. **Короткая память** — скользящее окно теряет контекст из начала интервью. Решение: добавить суммаризацию каждые 5 ходов.
2. **Зависимость от качества промптов** — агенты иногда генерируют общие вопросы при слабых инструкциях. Решение: усиленные промпты с анти-примерами (как в финальной версии).
3. **Ограниченная проверка фактов** — критик извлекает утверждения, но не проверяет их "на лету". Решение: интеграция с базой знаний (например, документацией Python/Java).

### 🚀 Потенциал применения
- Автоматизированный скрининг кандидатов перед живым интервью
- Обучающий симулятор для подготовки к техническим собеседованиям
- Анализ "слепых зон" знаний кандидата с персонализированным roadmap'ом
